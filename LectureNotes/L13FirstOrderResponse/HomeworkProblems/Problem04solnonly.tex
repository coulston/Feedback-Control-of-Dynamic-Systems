\begin{enumerate}[(a)]
	\setlength{\itemsep}{1pt}
	\setlength{\parskip}{0pt}
	\setlength{\parsep}{0pt}
	\item $t_r \approx 0.04$s, $t_s \approx 0.1$s 
	\item No short answer 
	\item No, the rise time and settling time from the plot are approximately 10x longer than expected from the transfer function, so the client did not provide the right combination.
\end{enumerate}

