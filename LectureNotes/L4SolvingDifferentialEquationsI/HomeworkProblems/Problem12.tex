A plant transfer function is given by 
$$\frac{F(s)}{R(s)} = \frac{3s+1}{s^{2}+4s+4}$$
If the input signal is a unit step function, that is, $r(t) = u(t)$, find the output signal f(t) in the time domain.

